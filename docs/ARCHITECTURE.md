# Registro-SismoLg26 — Guía para agentes

Guía de arquitectura, patrones y convenciones para trabajar en este repo sin explorarlo entero. Pensada para agentes de IA (Claude Code, Gemini Antigravity) y humanos nuevos. **Léela antes de escribir o modificar código.**

> 📉 **Costos de BD:** el análisis de **egress de Supabase** (dónde se consume, propuestas y lo ya aplicado) vive en [`docs/EGRESS.md`](./EGRESS.md). Consúltalo antes de tocar consultas/fetches de listas o estadísticas.

## Qué es

PWA **offline-first** de censo de afectados por sismo, para la **Gobernación del Estado La Guaira**. Operadores registran personas en **refugios**, con o sin señal (zona de desastre). Multi-refugio con permisos por rol.

## Stack

- **Next.js 16.2.9** App Router (`"use client"` en el árbol principal). ⚠️ Esta versión tiene breaking changes respecto a lo que "sabes": `params` es async (`await ctx.params`), `headers()` es async, route handlers no se cachean por defecto. **Lee `node_modules/next/dist/docs/` antes de escribir rutas o páginas.**
- **React 19**, **Prisma v7.8** + adaptador `pg` + **Supabase/PostgreSQL**.
- **IndexedDB** para offline (`src/lib/db.ts`).
- **Deploy: Vercel (serverless)** — el estado en memoria del servidor es efímero por-instancia (importa para el cache de sesión de auth).
- **CSS:** `src/app/globals.css` con variables (`--color-*`, `--element-height`) + clases semánticas (`.btn-submit`, `.user-role-badge`, `.dashboard-section`). Es el **sistema base**. Además hay **Tailwind v4 sin Preflight** para migración progresiva — ver sección "Tailwind" abajo.

## Estructura

- `src/app/page.tsx` — **ORQUESTADOR** (`Home`): todo el estado GLOBAL, los effects globales, monta `<AppContext.Provider>` y compone header + tabs + modales globales. ~800 líneas. **Regla GENERAL de modales:** `Home` llama `useModalOverlayScrollLock()` (`src/components/useBodyScrollLock.ts`) una sola vez → mientras exista CUALQUIER `.modal-overlay` en el DOM, el scroll del fondo se bloquea automáticamente (con compensación de scrollbar, sin salto). Todo modal nuevo **solo** necesita usar la clase `.modal-overlay` (con el contenido en `.modal-content`); NO hay que llamar a un hook por modal. `Home` también monta `useModalOutsideClickGuard()`: un click/selección que **empieza dentro** de `.modal-content` y **termina sobre el overlay** (p. ej. seleccionar texto y soltar afuera) **NO** cierra el modal —solo cierra si el gesto empieza en el overlay— (intercepta el click en fase de captura si el `mousedown` arrancó dentro). **Animación de SALIDA (sin saltos):** como React desmonta el modal al instante, usa el hook `useAnimatedModal(gate)` (`src/components/useAnimatedModal.ts`) para mantenerlo montado durante la animación de cierre. `gate` puede ser booleano (`showX`) u objeto (`xAEliminar`). Devuelve `{ mounted, closing, data }`: renderiza con `mounted`, añade `${closing ? " modal-overlay--closing" : ""}` al overlay y `${closing ? " modal-content--closing" : ""}` al contenido, y usa `data` (el último valor truthy, conservado durante la salida) en modales de confirmación para que el contenido no se quede sin datos. El handler de cierre solo pone el estado en cerrado como siempre. Para modales cuyo cierre TAMBIÉN limpia inputs del contenido (formularios de edición), usa el patrón de "cierre diferido" (un estado `xClosing` + `setTimeout(…, 220)` que limpia al final) para no vaciar el formulario durante la salida. CSS: `.modal-overlay--closing`/`.modal-content--closing` (keyframes `overlayFadeOut`/`modalOut`, 0.2s).
- `src/context/AppContext.tsx` — context híbrido (`AppContextValue`, `useAppContext()`).
- `src/tabs/` — una pestaña por archivo: `CensoTab` (wizard 4 pasos; controles 100% no-nativos —`StyledSelect`/`DatePicker`/`SearchableSelect`—; **validación con gating por `touched`**: un error solo se muestra tras tocar el campo o pulsar "Continuar"/"Enviar", así llegar a un paso no lo marca en rojo; radios validados por paso; **duplicado de cédula en vivo** al escribir vía `useMemo` sobre `registros`+locales, encadenado con el lookup del padrón y la precarga del jefe sin pisarse), `DashboardTab` (stats + reporte WhatsApp; **vista normal reformateada con el lenguaje `.bal-*`** —hero con badge de ícono, tarjetas con acento a color por métrica, donut/segmentos, ranking con medalla para parroquias, matriz responsive `.bal-matrix` y semáforo de ocupación por cuarto `.dash-rooms`; el radio pill viene de `--bal-radius` expuesto también en `.tab-view--dashboard`— + **modo presentación TV**: al entrar en pantalla completa —`requestFullscreen`— el contenido normal se reemplaza por `src/components/PresentationView.tsx`, un dashboard "control room" autorrotante con membrete + reloj/fecha en vivo, números con count-up, semáforo de ocupación por cuarto, **tema claro/oscuro elegible** (persistido en `localStorage` `pres_theme`, clase `.pres--light/--dark`) y **botón de pausa** de la autorrotación (el refresh de datos cada 5s sigue), 100% pill/responsive; recibe `stats/roomCounts/roomCapacities/allCuartos/refugio/onExit` por props, sin AppContext. La vista normal usa una **grilla de 2 columnas con filas de igual altura** (`align-items: stretch`) y paneles que tilean sin huecos —Salud y Alojamiento a ancho completo—), `AsignacionesTab` (tabla + filtros + detail/edición + modal de asignar habitación; la **tabla de registrados es responsive**: a ≥1024px se muestra como tabla completa y por debajo se transforma en **tarjetas apiladas** —cada fila una tarjeta, cada celda un `data-label` que rotula el valor— para que nunca aparezca un scroll horizontal interno; la lista fluye con la página; la **edición** precarga identidad al escribir/pulsar el botón sync dentro del input de cédula —censo+padrón—; **buscador + filtros avanzados** incl. grupo de edad + **rango de edad exacto (mín/máx)** y rango de fechas de registro; **"Exportar Excel"** genera un XLSX con membrete/colores de **lo filtrado** vía `src/lib/exportRegistrosExcel.ts` (el membrete lleva una 4ª línea que indica **si está filtrado y cómo** —o "Sin filtros — reporte completo"—; el componente arma el resumen `filtros` y lo pasa a la lib); en móvil los botones de barra colapsan a solo-icono con `.btn-txt-collapsible`), `UsuariosTab` (CRUD operadores), `ConfigTab` (**acordeón colapsable, 100% pill** vía `.config-acc*`: 5 paneles plegables —Perfil, Padrón, Sincronización, Salones, Campamentos— cada uno con cabecera ícono+acento por sección, badge de estado pill y chevron; el panel anima con `grid-template-rows` 0fr↔1fr —sin medir alturas en JS—; estado `openAcc` = `Set<string>` que permite varios abiertos y solo "perfil" abierto por defecto; cada sección aparece según rol —`canRegister`/`canManageRooms`/`isMaster`—. "Editar mi cuenta" abre un MODAL de autoservicio —edita SU nombre/contraseña; el correo NUNCA se cambia—. Formularios y modales internos van envueltos en `.pill-form` con controles no-nativos —`StyledSelect` para el V/E, nunca `<select>` nativo—), `MorbilidadTab` (registro clínico de consultas médicas offline-first; **"Nueva consulta"** abre un MODAL —botón en el header ANTES de los catálogos— con el flujo buscar-cédula→formulario; incluye **fecha-hora manual de la consulta** —`DatePicker`+`TimePicker`— y una sección **Lesiones/heridas/curas** del catálogo `TipoLesion`. La **tabla de historial** tiene contador `N de N`, buscador (nombre/cédula) y filtros avanzados (tipo/diagnóstico/estado físico) al estilo censo, sin columna de estado de sync, un botón **"Ver historial"** por fila que salta a Historial Clínico y abre a ese paciente vía `setPendingHistorialCedula`+`setActiveTab("historial")`, y **"Descargar Excel"** que abre un **modal con dos formatos**: (1) **Excel General** —XLSX con membrete institucional (logo, colores, encabezado de marca, **línea de filtros aplicados**, filas zebra, autofiltro) de lo filtrado, vía `src/lib/exportMorbilidadExcel.ts`—; y (2) **Registro Min Salud (SIS-02 · EPI-10/13)** —el formulario **oficial del MPPS**, **por día** (con `DatePicker`), vía `src/lib/exportRegistroMinSaludExcel.ts`. En vez de reconstruirlo se **rellena la PLANTILLA oficial** `public/plantillas/registro-min-salud.xlsx` (se carga con `exceljs`, se escriben los valores en sus celdas —anclas de las combinaciones— y se descarga → membrete/logos/combinaciones/leyendas quedan IDÉNTICOS). **TODO en MAYÚSCULAS**; DIAGNÓSTICO = patologías del diagnóstico **+ las notas del doctor**; NACIONALIDAD del prefijo de cédula (V/E); DIRECCIÓN del censo vinculado (`registroId`); CONSULTA P/S derivada (primera vs. sucesiva consulta de la cédula), X en blanco; INDÍGENA=NO y DIAGNÓSTICO=Confirmado por defecto; ESTADO=La Guaira / MUNICIPIO=Vargas fijos; ESTABLECIMIENTO=`Campamento Transitorio <refugio>`; Médico/ASIC/Parroquia/Tipo de consulta EN BLANCO (a mano). Es de **25 filas/página**: si el día supera 25 consultas se generan **varias hojas en el MISMO archivo** (la hoja de la plantilla se duplica a nivel del .xlsx con `jszip` —copia sheet+drawing/logos+rels y las registra— y luego ExcelJS rellena cada hoja). Ambas descargas quedan en los logs de auditoría (`logActivity`, cola offline). `exceljs` con **import perezoso**), `HistorialClinicoTab` (ficha clínica evolutiva: selector de paciente → banner demográfico + resumen —lista de problemas/diagnósticos, antecedentes cruzados con el censo, medicación— + **línea de tiempo** de todas sus consultas con diagnóstico/recetas/notas; 100% cliente desde consultas locales+remotas, sin BD nueva; clases `.hc-*`, responsive).
- `src/components/` — `AppHeader` (header+nav; **variante SIDEBAR** para escritorio: `AppSidebar` —sidebar lateral izq. flotante, **colapsable a solo-iconos**, 100% pill; se muestra solo en **≥1024px** (CSS `.app-sidebar`), donde además oculta `.app-header`/`.app-nav` y el `body` recibe `padding-left` para dejarle sitio —gated por `html[data-sidebar]` que pone el propio componente, así el login deslogueado no se recorre—; en <1024px sigue AppHeader intacto. El estado colapsado se persiste en `localStorage` `sidebar_collapsed`. Los ítems de nav de ambos salen de `src/components/navItems.tsx` (fuente única: etiqueta+icono+gating por rol, espeja el de AppHeader)), `LoginForm` (por props, fuera del Provider), `ToastIcon`, `CustomSelect`. **Controles con reformat** (sistema visual moderno "todo pill", altura única sincronizada): `SearchableSelect` (combobox buscable estilo "agregar"), `SearchableSingleSelect` (selector ÚNICO buscable: muestra el elegido dentro del control, no como píldora — se usa para el **cuarto**), `StyledSelect` (select sin buscador), `DatePicker` (calendario único), `TimePicker` (hora `HH:MM` con dos `StyledSelect`, paso de minutos configurable). **Adaptación integral a táctil:** en escritorio el desplegable se renderiza en un PORTAL anclado (`useAnchoredRect` → `fixed` desde el rect, con volteo y `visualViewport`, sin que lo recorte ningún `overflow`); en **teléfono/tablet** (`useIsMobile`, `max-width: 820px`) se abre como **MODAL nativo-like** (`MobileSheet`: hoja inferior con overlay, grip, título, cierre, scroll-lock y Esc; los buscables usan variante a pantalla con el campo de búsqueda pegado arriba). Aceptan prop `error`. **Regla:** en formularios nuevos usa estos, no `<select>`/`<input type=date>` nativos. **Sistema pill:** las clases `.censo-form` (registro) y `.pill-form` (reutilizable, p. ej. filtros y modal de edición de Registrados) comparten los tokens de altura/radio y estilan inputs/selects/botones/textarea como **pill** de altura única (`--ctl-h`); envuelve una sección con `.pill-form` para heredar todo el reformat. Búsquedas insensibles a acentos con `normalizeText`.
- `src/lib/` — `auth.ts` (guardas backend), `permissions.ts` (espejo cliente), `apiFetch.ts`, `db.ts` (IndexedDB + cola), `constants.ts`, `formReducer.ts`, `helpers.ts`, `prisma.ts`, `push.ts`.
- `src/types/index.ts` — tipos compartidos. `src/app/api/**/route.ts` — route handlers. `prisma/schema.prisma` — modelos.

## Patrón: estado y AppContext (híbrido)

El estado GLOBAL (`currentUser`, `isOnline`, `theme`, `registros`, `localRecords`, `customCuartos`, `stats`, `coords`, cola de sync…) y TODAS las funciones globales (`triggerSync`, `showToast`, `fetchRegistros`, `downloadFullPadron`…) viven en `page.tsx` (`Home`) con sus closures intactas, y se distribuyen por `AppContext`. Cada tab tiene su estado LOCAL en su propio componente. Cero prop-drilling.

**Excepción:** `LoginForm` se renderiza ANTES del Provider (return temprano cuando `!currentUser`) → recibe **props**, no context.

## Patrón: autorización (CRÍTICO)

**Regla de oro: el backend NUNCA confía en el rol/refugio que envía el cliente.**

- **Cliente:** `apiFetch` (`src/lib/apiFetch.ts`) añade el header `x-user-id` (del `localStorage`) + timeout con `AbortController`. **Úsalo SIEMPRE para llamar a `/api/`** — nunca `fetch` directo (excepto `/api/auth/login`, que es pre-sesión).
- **Servidor:** `getAuthUser(req)` (`src/lib/auth.ts`) lee `x-user-id` → busca el usuario REAL en la BD → deriva rol y refugio. Todas las guardas parten de ahí. Helpers: `isMaster`, `canRegister`, `canDeleteRegistro`, `canManageUsers`, `canManageRooms`, `canManagePadron`, `canManageTargetUser`, `canActOnRefugio`, `refugioScope`, `hasRefugio`. Cache de sesión en memoria (TTL 30s) + `invalidateSession` al editar/borrar.
- `src/lib/permissions.ts` es el **espejo cliente** (mismas reglas por `role` string) — SOLO para UX (mostrar/ocultar botones). El backend es la verdad.

## Roles y refugios

- **Refugio = campamento.** `User.campamentoTransitorio` = refugio del operador; `Registro.refugio` = refugio del registro; tabla `Refugio` = lista canónica (CRUD solo Master); `CustomRoom.refugio` = cuartos por refugio.
- **MASTER:** todo, todos los refugios. Se gestionan **por SQL** (nadie los crea/edita/borra desde la app; MASTER no es rol asignable en la UI).
- **ADMIN:** administra SU refugio (usuarios Reg/Vis de su refugio, registros, cuartos, stats de su refugio). No toca otros Admin ni Master. **Solo Master asigna Admin.**
- **REGISTRADOR:** censa + edita registros de su refugio; usa el padrón para autocompletar.
- **VISUALIZADOR:** solo ve y exporta su refugio.
- **Roles MÉDICOS** (`isMedico` = AdminMedico | OperadorMedico | AsistenteMedico): **solo ven Morbilidad, Balance de Salud e Historial Clínico** (no censo, dashboard general, Registrados ni Configuración general). Gating en `AppHeader` + `page.tsx` (render + arrays `allowed` del efecto de redirección — un tab nuevo médico debe agregarse a AMBOS o rebota a Morbilidad). Añadir una pestaña = editar `ActiveTab` en `types/index.ts`, el render en `page.tsx`, los `allowed` del efecto, y 3 puntos en `AppHeader` (botón desktop con `data-tab`, label móvil activo, ítem del dropdown móvil). No hay registro central de tabs.
  - **Balance de Salud** (`src/tabs/BalanceTab.tsx`, gated `canManageMorbilidad`): tablero de morbilidad calculado en el cliente desde las consultas (locales + remotas) **cruzadas con el censo** (`registros`) cuando falta género/edad. Cards de totales (incl. **"Mujeres embarazadas"** = pacientes con el estado explícito `embarazo="SI"` en la consulta o el censo; con respaldo por palabras clave para datos legados sin el campo), barras por género y edad, matriz edad×género y top patologías. **Grupos de edad:** Lactantes (0–3), Menores (4–17), Adultos (18–59), Mayores (≥60), + Sin edad (`bucketOf`). Reusa el estilo del dashboard (`.bal-*`) y es responsive (matriz → tarjetas en móvil).
  - **AdminMedico:** además ve **Usuarios** pero FILTRADO a solo médicos (Operador/Asistente) de su refugio; crea/edita SOLO OperadorMedico y AsistenteMedico (crear AdminMedico es exclusivo de Master). Elimina catálogos.
  - **OperadorMedico:** crea/edita consultas (no elimina) y **crea/edita catálogos** (patologías/medicamentos, sin eliminar).
  - **AsistenteMedico:** solo crea/edita consultas; NO toca catálogos ni elimina nada.
- **Catálogos médicos** (patologías/medicamentos): la gestión se hace desde **Morbilidad** (componente `src/components/CatalogosMedicos.tsx`: 2 botones + modales, full pill). Permisos: `canEditCatalogosMedicos` (Master/AdminMedico/OperadorMedico → crear + editar-renombrar vía PUT conservando el ID) y `canManageCatalogosMedicos` (Master/AdminMedico → eliminar). Espejados en `permissions.ts` (cliente) y `auth.ts` (backend, fuente de verdad). Ya **no** están en Configuración.
- **Gestión de usuarios:** `canManageUsers` = Master/Admin/AdminMedico. `assignableRoles(actor)` y `canManageTargetUser(actor, target)` (en `auth.ts` + espejo en `permissions.ts`) definen quién asigna/gestiona qué rol; el selector de rol de `UsuariosTab` se puebla desde `assignableRoles` (no roles hardcodeados). El listado GET se filtra por actor (`usersListWhere`).
- **Consultas médicas:** son **editables** (botón "Editar" en el historial → modal con el **mismo layout que el formulario de crear** —Datos Básicos + Antecedentes | Diagnóstico en 2 columnas—, `.modal-content--morb` que **aprovecha el ancho en PC** y es 100% responsive). Editar re-encola la consulta local con su MISMO id (`saveLocalConsulta`, preservando `createdAt`) y sincroniza; el POST de `/api/consultas` hace **upsert-update** (crea si es nueva, actualiza si el id existe). Permiso = `canManageMorbilidad` (Operador/Asistente/AdminMedico/Master crean y editan; **nadie elimina**). Reenviar datos idénticos no genera diff → el trigger de auditoría no registra cambio.
- **Consultas por refugio (scoping, crítico):** el `refugio` de la consulta lo decide el **backend**, no el cliente — Master respeta el refugio de vista (o el suyo), el resto se **fuerza a su propio refugio** (igual que `/api/register`). Así toda consulta queda vinculada al refugio de quien la crea. **Ver** = `refugioScopeFor` (cada quien solo lista las de su refugio); **editar** = guarda `canActOnRefugio(auth, existing.refugio)` (no master → solo las de su refugio; editar mantiene el refugio original). No hay borrado de consultas.
- **Edad en Morbilidad:** NUNCA se ajusta a mano; se **deriva de la fecha de nacimiento**. Se persiste `ConsultaMedica.fechaNacimiento` (yyyy-mm-dd) para poder editarla luego (DatePicker) y recalcular la edad. Consultas viejas sin fecha: el modal de edición la recupera del censo (por UID/cédula) como respaldo. SQL: `prisma/migrate_consulta_fechanacimiento.sql` (idempotente, correr en Supabase).
- **Estados EXPLÍCITOS del paciente (Estado físico ileso/lesionado + Embarazo) — toggles con auto-sugerencia + override:** en la consulta el médico ve **toggles segmentados pill** (`estadosRow` / `.morb-seg`): Estado físico `[Ileso|Lesionado]` y, **solo en mujeres**, Embarazo `[No|Sí]`. Se **auto-sugieren** (Lesionado si hay lesión activa en la sección Lesiones o patología de trauma en antecedentes/diagnóstico — `hasInjurySignal`/`INJURY_KEYWORDS`; Sí si hay patología cuyo nombre contiene "embarazo" — `isEmbarazoPatologiaId`), pero **una vez que el médico toca el toggle, su decisión manda** (flag `touched`). La "base" al cargar = el valor actual del censo (`estadoBaseRef`/`embarazoBaseRef`), para no revertir un lesionado/embarazo del triaje si la consulta no trae señal. El valor elegido se guarda en la **consulta** (`ConsultaMedica.estadoFisico`/`.embarazo`) y se **sincroniza al censo** (`Registro.estadoFisico`/`.embarazo`) vía `syncPatientToRegistro`/`syncEditToRegistro` (→ `POST /api/register`). En el modal de edición se detecta si el valor guardado fue un override (difiere de la auto-sugerencia) para respetarlo. Pacientes NO censados (apoyo externo) no actualizan censo. **Las palabras clave ya NO son la verdad** — solo alimentan el valor por defecto del toggle; Balance/Estadísticas leen el campo explícito. SQL: `prisma/migrate_estado_embarazo.sql`.
- **Scoping:** `refugioScope(user)` → Master `{}` (todo), resto `{ refugio }`. Aplicado en registros, stats, cuartos, usuarios.

## Patrón: offline (señal casi nula)

- `src/lib/db.ts`: IndexedDB. Cola `LocalRegistro` (`status` pending/synced/error, `type` new/update, `attempts`, `nextAttemptAt`, `refugio`, `userId`).
- Al censar/editar/asignar: `saveLocal` (encola) → sincroniza cuando hay señal. Se **sella `refugio`+`userId`** en el registro offline.
- `triggerSync` (page.tsx): cada 15s + evento `online` + mount. Lotes de 2. **Prioriza censos NUEVOS** sobre ediciones. `401/403/400` → `markPermanentError` (no reintenta, avisa); red/`5xx` → `incrementAttempt` con **backoff exponencial** (15s→5min); `getPending` respeta el backoff. `apiFetch` con timeout.
- **Padrón electoral:** se descarga a IndexedDB (streaming NDJSON) para lookup offline de cédulas. **Reanuda** en cada arranque comparando el conteo local vs `/api/padron/count`. Lo descarga cualquiera que cense (`canRegister`); subir el CNE es solo Master/Admin.
- **Identidad por cédula — 3 fuentes en cascada:** (1) censo local (otro registro), (2) padrón local (IndexedDB, offline), (3) **API externa `api.cedula.com.ve`** (online). La #3 se consume vía **proxy backend** `/api/cedula-lookup` (autenticado), evita CORS y **normaliza** la respuesta: nombre `NOMBRES + APELLIDOS`, `fecha_nac` (yyyy-mm-dd), género solo si viene; ignora cne/estado/municipio/parroquia/rif. **Credenciales:** el `app_id`/`token` están **hardcodeados a propósito** (`DEFAULT_APP_ID`/`DEFAULT_TOKEN`) — **excepción consciente a la regla anti-hardcode, autorizada por el dueño**: `api.cedula.com.ve` es un servicio LIBRE (~2000/h) y su difusión no preocupa; hay override opcional por env (`CEDULA_API_APP_ID`/`CEDULA_API_TOKEN`) por si se rota. **No "corregir" quitando los defaults.** Cliente: helper `fetchCedulaExterna` (`src/lib/cedulaApi.ts`, solo online, degrada a null). Se dispara **automático al escribir** (tras fallar local) **y con el botón ⟳**, en **Censo (registro), Registros (edición) y Morbilidad**. Para **hijos/dependientes** NO se consulta (comparte la cédula del representante).
- **Hijos/dependientes sin cédula propia:** se registran con la cédula del REPRESENTANTE + un sufijo correlativo → `"<nac>-<dígitos>-<N>"` (p. ej. `V-12345678-1`). El flag `isChildDependent` + `dependentNumber` es solo de UI (no hay columna en BD; se guarda la cédula final con sufijo). **Reglas (censo y edición):** (1) el chequeo de duplicado compara la cédula COMPLETA con sufijo —así un hijo no se cruza con el padre—; (2) si es hijo, NO se consulta el padrón (devolvería al representante), y el lookup del censo usa la cédula con sufijo. En `AsignacionesTab` el helper `parseStoredCedula` descompone la cédula guardada al abrir la edición (reconstruye el check + correlativo y deja solo los dígitos en el input); `handleSaveEdit` vuelve a armar la cédula CON sufijo. En `CensoTab`, `triggerLookup` (padrón) se omite cuando `isChildDependent`.
- **Cache local** (registros/stats en `localStorage`) sellado por dueño (`cached_owner`) → no filtra datos entre refugios en dispositivo compartido; se limpia en logout.

## Modelo de datos (`prisma/schema.prisma`)

- **Registro:** datos del afectado + `refugio` + `cuarto` + `retirado` + `intermitente` + `cedulaJefeFamilia` + `estadoFisico` ("ILESO"/"LESIONADO") + `embarazo` ("SI"/"NO", default "NO"; estado explícito editable en el **censo** (registro y edición) y en **Asignaciones**, y sincronizado desde Morbilidad — el control solo aparece si `genero === "FEMENINO"`). **Salud por-ID:** `patologiaIds` (Json, ids de `Patologia`) + `medicamentoIds` (Json, `[{id,dosis,periodo}]` que referencian `MedicamentoPredefinido`). Las columnas viejas `patologiaDescripcion` (texto) y `medicamentos` (`[{nombre,…}]`) quedan **congeladas** como respaldo (se dropean tras validar el backfill). **Unicidad de cédula: POR CAMPAMENTO, no global** — `@@unique([cedula, refugio])` (antes era `cedula @unique` global). Una persona puede tener **una fila por refugio**: retirada en el de origen + activa en el destino. SQL: `prisma/migrate_cedula_por_refugio.sql` (idempotente, correr en Supabase). **Traslado automático** (en `/api/register` POST): al crear en el campamento Y a alguien ACTIVO en otro campamento, en la MISMA transacción se retira allá (`retirado=SI`, razón "Trasladado al campamento Y") y se crea aquí; la respuesta trae `transferredFrom: string[]`. Nunca quedan dos filas ACTIVAS de la misma cédula. Duplicado en el MISMO refugio: activo → 409 `DUPLICATED`; retirado → 409 `RETIRED_HERE` (reactivar desde Registros). **Regla invariante "≤1 fila ACTIVA por cédula"**: al EDITAR, si la fila queda activa (incluye REACTIVAR `retirado→NO`) y esa cédula ya está activa en otro campamento → 409 `ACTIVE_ELSEWHERE` ("retírala allá primero") — no se hace traslado inverso automático, se bloquea para que un humano reconcilie. El match de paciente en Morbilidad usa los `registros` scoped al refugio, así que toma la fila activa correcta sin cambios; **el historial clínico es POR CAMPAMENTO** (cada refugio ve sus consultas). **`PATCH /api/registros/[id]` fue ELIMINADO** (quedaba huérfano y saltaba estos guards; los edits van por la cola → `/api/register`); esa ruta solo conserva `DELETE`.
- **User:** `email`, `nombre`, `password` (scrypt), `role` ("MASTER" | "ADMIN" | "REGISTRADOR" | "VISUALIZADOR" | "AdminMedico" | "OperadorMedico" | "AsistenteMedico"), `campamentoTransitorio` (= refugio).
- **Refugio:** `id`, `nombre` @unique, `ubicacion` (URL de Maps, opcional; editable en Config y usada en el reporte de WhatsApp del refugio activo). **CustomRoom:** `name`, `refugio`, `capacidad` (camas, `Int @default(18)`), `@@unique([name, refugio])`.
- **Padron:** cédulas del CNE (lookup offline). **PushSubscription:** web push. La lógica de cliente vive en `src/lib/pushClient.ts` (`enablePush`/`pushSupported`/`pushPermission`, clave VAPID pública): la usa el arranque (auto para ADMIN en `page.tsx`) **y** Configuración, donde hay un botón **Activar/Renovar** para (re)solicitar el permiso y (re)suscribir a mano (gesto del usuario → el prompt aparece aunque antes no se mostrara). Si el permiso está **bloqueado**, JS no puede reabrir el prompt: se muestra una ayuda para desbloquear en el navegador + "Volver a comprobar". Gated a ADMIN + Master. **Payload de la notificación (server → `src/lib/push.ts`, disparado por `register`):** el cuerpo termina con `Campamento: <refugio>` (el Master ve dónde se registró sin abrir la app) y lleva un **`tag` ÚNICO por registro** (`nuevo-registro-<refugio>-<id>`). El SW (`public/sw.js`, handler `push`) usa ese `tag` con `renotify:false` → las notificaciones se **APILAN** (no se reemplazan entre sí), agrupadas por campamento. **No volver a un `tag` fijo compartido** (colapsaría todas en una).
- **Patologia:** catálogo canónico de patologías (`id`, `nombre` @unique). Se referencia por **id** desde censo/consultas.
- **TipoLesion:** catálogo administrable de tipos de lesión/herida (`id`, `nombre` @unique), análogo a `Patologia`. Se referencia por **id** desde `ConsultaMedica.lesiones`. Gestionado por AdminMedico/Master desde Morbilidad → Catálogos.
- **ConsultaMedica:** consulta de morbilidad (datos básicos + `registroId` = **UID** del censo vinculado + antecedentes/diagnóstico **por-ID** en columnas JSON `*PatologiaIds`/`*MedicamentoIds` + notas). **`fechaConsulta`** (`DateTime?`) = fecha-hora REAL de la atención **elegida a mano** (distinta del `createdAt` de carga; si falta se usa `createdAt`). **`lesiones`** (`Json`, `[{tipoId,zona,estado,cura}]`): tipo por-ID de `TipoLesion`, `zona` de `ZONAS_CUERPO`, `estado` ∈ `ESTADO_LESION_OPTS` (NUEVA/EN_TRATAMIENTO/INFECTADA/CICATRIZADA), `cura` texto libre. **`estadoFisico`** ("ILESO"/"LESIONADO") y **`embarazo`** ("SI"/"NO") = estados explícitos elegidos en los toggles (se sincronizan al `Registro`). Las columnas de texto viejas quedan congeladas como respaldo. SQL: `prisma/migrate_lesiones.sql` + `prisma/migrate_estado_embarazo.sql` (idempotentes, correr en Supabase).
- **MedicamentoPredefinido:** catálogo de medicamentos. Único por **`(nombre, concentracion, presentacion)`** (un principio activo se repite con distinta presentación); `dosis`/`periodo` son sugerencias opcionales.
- **Caracterización (encuesta socioeconómica por familia — Fase 1):** módulo que **extiende el censo sin duplicarlo**. Reutiliza `Registro` (identidad/ubicación/GPS/salud básica + vínculo familiar) y solo captura lo NUEVO, con preguntas **cerradas** de un **catálogo general** (`CaracterizacionOpcion`: una tabla para todas las listas, agrupada por `modulo`/`campo`, referenciada por-ID). Dos niveles: **`CaracterizacionHogar`** (1 por familia, anclada a `jefeRegistroId` = UID del `Registro` del jefe) y **`CaracterizacionPersona`** (1 por censado, `registroId` @unique + `familiaCedula` para traer la ficha completa) → los datos por-persona (grupo sanguíneo, tallas, oficio…) quedan **consultables**. Todos los `*Id`/`*Ids` referencian el catálogo; los SI/NO son strings. **UI:** pestaña `CaracterizacionTab` (gating `canRegister`; lista las **familias del censo** agrupadas por `family_id` con estado de cobertura Sin ficha/Parcial/Completa + `Pagination`) → abre `CaracterizacionFicha` (modal pill: sección Hogar una vez + tarjeta por miembro precargada del censo; campos generados desde `CARAC_CAMPOS` en `constants.ts`). Gestión del catálogo: `CaracterizacionCatalogos` (guarda `canManageCaracterizacion` = Master/Admin). **Offline:** store IndexedDB `caracterizacion` (DB **v6**), 1 registro = ficha de una familia (`{hogar, personas}`), sync en `triggerSync` → `POST /api/caracterizacion` (upsert en transacción, refugio forzado). **SQL (manual, idempotente):** `prisma/caracterizacion_migration.sql` (tablas+índices+trigger `syncedAt`) y `prisma/seed_caracterizacion_opciones.sql` (listas cerradas). **Estado:** captura completa de los **8 módulos** (Fase 1 + Fase 2 socioeconómico/laboral, ambos ya expuestos en la ficha). **Pendiente:** un reporte/export (Excel) de caracterización.

## Rutas API (`src/app/api`)

`auth/login` (pre-sesión), `auth/users` (GET/POST/PUT/DELETE con guardas por rol+refugio), **`auth/me`** (PUT autoservicio: el usuario autenticado edita SOLO su propio `nombre` y/o `password` —cambiar contraseña exige la actual, verificada en el back—; opera SIEMPRE sobre `auth.id` de la sesión, NUNCA sobre un id del body; nunca toca email/rol/campamento), `registros` (GET scoped), `registros/[id]` (PATCH/DELETE), `register` (crea/actualiza censo, fuerza el refugio del operador; **guard de cédula duplicada**: al CREAR rechaza con `409 DUPLICATED` si ya existe un registro ACTIVO —no retirado— con esa cédula, además del índice `@unique` como backstop; el sync trata 409 como "duplicado"), `stats` (scoped), `cuartos` (GET/POST/PATCH/DELETE scoped por refugio; **PATCH edita un salón —nombre y/o capacidad—**; si cambia el nombre, reasigna en cascada todos los `Registro` de ese refugio que estaban en el salón, en UNA transacción atómica vía `withAuditUser` (o todo, o nada); GET ordena por `createdAt asc` + desempate estable por `id` → los primeros creados arriba y sin reordenarse al hacer UPDATE), `refugios` (CRUD Master, renombra en cascada), `padron/download|count|upload-cne`, `public-search` (pública; busca en `cedula` **y** `cedulaJefeFamilia`), `external-search` (pública; fuentes externas, ver abajo), `lookup`, `push/subscribe`. `public-search`, `external-search` y la página `/buscar` son públicas.
- **Correo (nodemailer + Gmail App Password)** — `src/lib/mailer.ts` (`sendMail`, `renderEmail`, `mailerReady`, `alertEmails`; env `GMAIL_USER`/`GMAIL_APP_PASSWORD`/`MAIL_FROM`/`ALERT_EMAILS`; opc. `APP_URL`/`NEXT_PUBLIC_APP_URL` para el botón "Ingresar" del correo de bienvenida). **Degradación segura:** sin credenciales no envía ni bloquea nada. **OTP** (`src/lib/otp.ts` + modelo `VerificationCode` — hash sha256, expira 10 min, 5 intentos, **cooldown 60s** que reutiliza el código vigente en vez de reenviar, **limpieza** de vencidos/consumidos en cada `requestCode`, y **auditoría** de fallos en logs): `auth/users` (POST/PUT/**DELETE** —en DELETE el código viaja por query `?challengeId=&code=`) y `auth/me` (cambio de contraseña) pasan por `otpGate`/`otpErrorResponse` → si el correo está activo y falta el código, envían un código al **ACTOR** y responden **`403 CODE_REQUIRED { challengeId }`**; el cliente reintenta la MISMA acción con `{ challengeId, code }` (componente `OtpModal` en `UsuariosTab`/`ConfigTab`; el borrado pasa por `runUserAction`). Si `!mailerReady()` → OTP **omitido** (opera como hoy). `auth/otp/request` (re)envía el código al usuario autenticado. **Recuperar contraseña (SIN sesión)** — `auth/password-reset/request` (`{email}` → envía código con purpose `PASSWORD_RESET`; **no revela si el correo existe**: responde igual exista o no, y `{available:false}` si el correo no está configurado) + `auth/password-reset/confirm` (`{email,code,newPassword}` → `verifyCodeByEmail` valida el código VIGENTE del correo, sin `challengeId`, y fija la contraseña; no exige la actual). UI: enlace "¿Olvidaste tu contraseña?" en `LoginForm` → `ResetPasswordModal` (2 pasos, pill). **Avisos** fire-and-forget a `ALERT_EMAILS`: **traslado** entre campamentos (`register`) y **usuario nuevo** (`auth/users`). **Bienvenida** al NUEVO usuario (`auth/users` POST): correo con su acceso (correo/rol/campamento) que apunta a "¿Olvidaste tu contraseña?" para fijar su clave (no se envía contraseña en claro). SQL: `prisma/verification_code_migration.sql` (mismo de antes; recuperación/cooldown/limpieza NO requieren migración nueva). (El script dev `scripts/send-push-email.mjs` sigue en Resend — un solo destinatario, el dueño.)
- `patologias` (GET devuelve `{id,nombre}[]`; POST/DELETE gestionan el catálogo con guarda `canManageCatalogosMedicos`). **Sin auto-seed.**
- `medicamentos` (GET catálogo con `id`; POST/DELETE con guarda `canManageCatalogosMedicos`). **Sin auto-seed.**
- `tipos-lesion` (GET devuelve `{id,nombre}[]`; POST/PUT con `canEditCatalogosMedicos`, DELETE con `canManageCatalogosMedicos`). Catálogo `TipoLesion`; mismo patrón que `patologias`. **Sin auto-seed.**
- `caracterizacion` (GET **estado ligero** scoped por refugio —qué familias tienen hogar y qué personas tienen ficha— con **ETag/304**; POST **upsert** de la ficha —hogar + N personas— en una transacción, refugio forzado; **POR AHORA todo el módulo es solo Master** —tab + POST + catálogo—), `caracterizacion/[jefeRegistroId]` (GET ficha completa para editar), `caracterizacion/opciones` (catálogo general: GET cache 120s; POST/PUT/DELETE con `canManageCaracterizacion` = solo Master por ahora). **Sin auto-seed** (listas por SQL manual).
- `monitoreo` (**solo Master**): GET con números GENERALES por campamento, **todo agregado en SQL** (`GROUP BY refugio` en `src/lib/monitoreo.ts` → activos/presentes/intermitentes/retirados, núcleos, ocupación asignados/capacidad, lesionados/patología/embarazadas, consultas, fichas) → egress mínimo (no trae filas). **ETag/304** (`monitoreoETag`: count+max(syncedAt) del censo + count de consultas/salones). Pestaña `MonitoreoTab` (tabla comparativa → tarjetas en móvil): **sin auto-refresh** (carga al abrir + botón "Actualizar") + cache local, para no consumir Supabase.
- `consultas` (GET scoped; POST con guarda `canManageMorbilidad`; **DELETE `?id=`** con guarda `canDeleteConsulta` —solo Master y AdminMedico— + `canActOnRefugio`; guarda antecedentes/diagnóstico **por-ID** + `registroId` + **`fechaConsulta`**, **`lesiones`**, **`estadoFisico`** y **`embarazo`** saneados). En el cliente el borrado tiene **modal de confirmación**; una consulta local no sincronizada se borra solo del store local (`deleteLocalConsulta`). **Propagación al censo (server-side):** al crear/editar una consulta vinculada a una persona del censo (`registroId`), el POST copia los estados **explícitos** de la consulta al `Registro` —`estadoFisico` siempre; `embarazo` solo en mujeres—, sin pisar con nulos, en `try/catch` (best-effort; no rompe la consulta si el registro no existe o la columna no está migrada). Así la **evolución clínica mantiene el censo al día automáticamente**, aun editando consultas fuera del flujo de búsqueda. Complementa al sync cliente (`syncPatientToRegistro` en `MorbilidadTab`, que además propaga nombre/género/edad/antecedentes). **Backfill histórico:** `prisma/backfill_embarazo_from_consultas.sql` (idempotente, manual en Supabase) recorre las consultas por cédula y marca `Registro.embarazo='SI'` según la evolución (última consulta explícita manda; respaldo por patología de "embarazo"); solo añade, nunca desmarca.
- **`stats`** ahora es un *thin wrapper*: la agregación vive en `src/lib/stats.ts` (`computeAggregateStats(scopeRefugio)`), **fuente única** reusada también por el reporte público. **Grupos de edad:** incluye `lactantes` (0–3) y `matrix.lactantes` como **subconjunto de `menores`** (que se mantiene <18, sin romper el reporte de WhatsApp «Niños» ni consumidores previos). Estadísticas (Dashboard), modo presentación y reporte público muestran el **desglose de 4 grupos** —Lactantes (0–3) / Menores (4–17 = `menores − lactantes`) / Adultos / Mayores— y **replican los gráficos de Balance**: barra segmentada `.bal-seg` (por edad) y donut `.bal-donut` (por género). También expone `embarazadas` (censo, `Registro.embarazo = 'SI'`) con un **conteo AISLADO y a prueba de fallos** (`try/catch` con `prisma.registro.count` separado del `$queryRaw` principal): si la columna `embarazo` aún no está migrada en producción, degrada a 0 **sin romper** el resto de estadísticas ni el reporte público. **Modo presentación** (`PresentationView`): 5 slides autorotativas —Panorama (Personas registradas total + Presentes + Intermitentes…), Demografía (edad/género/matriz), **Distribución Territorial** (`.pres-terr`, panel doble: ranking de parroquias + dona de "Concentración territorial" = Top 3 parroquias vs resto), Salud y Condición (Con patología / Lesionados / Mujeres embarazadas + barras "Con/Sin patología" y "Lesionados/No lesionados"), Alojamiento (semáforo por cuarto)—. Cubre TODO lo del Panel de Estadísticas.
- **Reporte público por link** (`reporte/*`, modelos `ReporteCompartido` + `ReporteAcceso`, SQL `prisma/migrate_reporte_compartido.sql`):
  - `POST /api/reporte` (auth) crea un link; Master **elige el refugio** (o `null` = consolidado), el resto queda **forzado a su refugio**. `GET` lista con **matriz de visibilidad** (`canSeeLink`): Master ve TODOS, Admin ve **solo los de SU refugio** (y nunca los de un Master), el resto solo los suyos (devuelve `creadoPorNombre`/`creadoPorRole`/`esMio`/`puedeRevocar` — rol vía join a `User`). `DELETE ?id=` revoca (soft, `activo=false`) con **matriz de revocación** (`canRevokeLink`): los propios siempre; **Master revoca TODOS**; Admin revoca los de usuarios **no privilegiados de su refugio** (nunca de otro admin ni de un master). En el modal (Dashboard) hay **copiar por fila** (re-copia cualquier link activo) y scroll-lock de fondo.
  - `GET /api/reporte/[token]` (**público, sin auth**) devuelve metadatos (refugio, quién lo compartió, ubicación GMaps) **+ estadísticas AGREGADAS** (`computeAggregateStats`, sin PII). La ubicación NO es requisito para ver el reporte.
  - **Demografía por edad SIN huecos ni solapes** (fuente única en `src/lib/stats.ts` + espejo offline en `DashboardTab.getLocalStats`; ambos deben coincidir): **lactantes 0–3, no lactantes 4–12, adolescentes 13–17, adultos 18–59, mayores 60+**, cada categoría con matriz por género. (`menores` = <18 se conserva como agregado.)
  - **Reporte de WhatsApp** (Dashboard, modal): 2 tipos elegibles — **Resumen** (el clásico) y **Detallado** (encabezado con campos libres *estado/municipio/organismo responsable* —persistidos en `localStorage`— + **ubicación** (link GMaps del refugio, bajo el nombre) + indicadores de capacidad/ocupación + demografía por las 5 categorías de edad × género + **núcleos familiares** + **novedad** —lesionados, patologías crónicas, embarazadas—). Estado (**La Guaira**) y municipio (**Vargas**) son **fijos** del sistema —hardcode institucional autorizado por el dueño—; el único campo libre es el organismo responsable. Todo pill/responsive.
  - `POST /api/reporte/[token]/acceso` (**público, solo auditoría**) registra cada apertura en `ReporteAcceso`: IP (vía `x-forwarded-for`), `user-agent` y ubicación **si la conceden** (`ubicacionConcedida`). Best-effort (nunca rompe la vista); no devuelve estadísticas.
  - Página pública `src/app/reporte/[token]/page.tsx` (fuera del shell de la app): muestra el reporte **de inmediato** (fases `loading → ready`, sin compuerta) y, en **2do plano**, pide la geolocalización y llama a `/acceso` una sola vez (con o sin ubicación) para auditar. Tema claro por defecto + toggle discreto (`data-theme`). Render de solo lectura con `src/components/PublicReportView.tsx` (lenguaje `.bal-*`, sin PII). Se comparte desde el botón "Compartir" del Panel de Estadísticas. **Contenido:** replica TODO lo del Panel de Estadísticas —mismo set de tarjetas (incluye Intermitentes, Mujeres embarazadas, Por asignar), edad, género, matriz edad×género, parroquias y "Salud y condición física" (estado físico + patologías)— **EXCEPTO el desglose cuarto-por-cuarto** (no viaja `roomCounts` en las stats agregadas, así que no es representable).

## Esquemas de trabajo (cómo trabajar aquí)

- **Antes de cada commit:** `npx tsc --noEmit` limpio. `tsc` valida tipos, NO comportamiento — para runtime, correr la app.
- **Nada de hardcode:** no incrustes valores fijos (refugios, nombres, capacidades, listas, credenciales, lo que deba venir de BD/config/estado/`.env`). Si encuentras hardcode existente, **adviértelo y propón solución** en vez de dejarlo pasar. Ej. resueltos: el refugio "Complejo Educativo…" salió del censo, login, reporte de WhatsApp, búsqueda y de los `@default` del schema; el auto-seed de salones se eliminó.
- **PROHIBIDO auto-seed de catálogos:** nunca detectes una tabla vacía y la llenes por código (`if (count === 0) createMany(...)`). Los catálogos (`Patologia`, `MedicamentoPredefinido`, `TipoLesion`) se cargan por **SQL manual idempotente** entregado al dueño (la semilla dentro de un SQL manual SÍ está permitida; lo prohibido es que el código la siembre), o desde Config (AdminMedico). Los seeds viven en `prisma/seed_patologias_cie.sql` y `prisma/seed_medicamentos.sql`; el backfill de datos legados nombre→id en `prisma/migrate_ids_backfill.sql` (match exacto; no coincidentes → `prisma/report_unmatched.sql`).
- **Catálogos médicos por-ID (patrón):** censo/consulta/edición **solo eligen del catálogo** (nada de texto libre). Se guarda el **id** (`patologiaIds`, `medicamentoIds` con `{id,dosis,periodo}`); para mostrar/exportar se interpola el nombre con `patologiaNombre`/`patologiaNombres`/`medLabel`/`medItemsText` (`src/lib/helpers.ts`). La consulta se vincula al censo por **UID** (`ConsultaMedica.registroId`). Al cambiar el shape se **subió la versión** de los caches (`sismo_cached_patologias_v2`, `cached_consultas_v2`). **Despliegue:** vaciar la cola offline pendiente antes del corte (registros encolados con el shape viejo no traen ids).
- **Next 16:** `params`/`headers()` async; leer `node_modules/next/dist/docs/` antes de rutas/páginas.
- **Commits por fase**, descriptivos. Se trabaja en `main` (preferencia del dueño), con cuidado y verificación por fase.
- **Antes de cada `push`:** `git pull --rebase origin main` para integrar el trabajo de otros devs y no sobrescribir ni omitir sus cambios; resolver conflictos antes de pushear.
- **Mantén esta guía viva:** consúltala al **iniciar** cualquier tarea (partir del estado real), y **actualízala al terminar un cambio importante** (nuevo patrón, ruta, dependencia, convención o decisión de arquitectura). La usan Claude y Gemini Antigravity — si aplica, actualiza también el skill de Claude (`.claude/skills/registro-sismo/SKILL.md`). No la dejes desactualizada.
- **Migraciones de BD (manual + idempotente):** todo cambio de `prisma/schema.prisma` → actualizar el schema + `npx prisma generate`, y **entregar al dueño el SQL de migración** para ejecutarlo **manualmente en Supabase**. NO se corre `prisma migrate`/`db push` automático contra producción. El SQL debe ser **idempotente** (re-ejecutable sin romper si ya se aplicó): `CREATE TABLE IF NOT EXISTS`, `ALTER TABLE ... ADD COLUMN IF NOT EXISTS`, `DROP CONSTRAINT IF EXISTS`, `INSERT ... ON CONFLICT DO NOTHING`, `UPDATE ... WHERE`.
- **Auditoría (Registro y User):** un trigger de BD (`prisma/audit_setup.sql`) registra cada CREATE/UPDATE/DELETE en la tabla `AuditLog` (CREATE/DELETE = fila completa **sin** `password`; UPDATE = solo el diff, ignora `syncedAt`). Toda escritura de `Registro`/`User` en la API va **envuelta en `withAuditUser(auth.email, tx => tx.…)`** (`src/lib/audit.ts`) — setea `app.user_email` local a la transacción para que el trigger registre el correo del operador; sin eso cae al rol de BD (`db_role`). El SQL se ejecuta **manual en Supabase**. **Acciones de usuario (imprimir/exportar):** además de los cambios de datos, la app registra en el MISMO `AuditLog` cada impresión de PDF y descarga de Excel vía `POST /api/activity-log` (`accion` = `PRINT`/`EXPORT`, `entidad` = `Impresion`/`Exportacion`, `metadata` = recurso/formato/refugio/filtros/total/rol; helper `src/lib/activityLog.ts`, nunca bloquea la acción; inserción directa con `$executeRaw` porque `AuditLog` no está en Prisma). **Cola OFFLINE propia** (store `activity_logs` en IndexedDB, `src/lib/db.ts`; INDEPENDIENTE de la cola de registros): `logActivity` PERSISTE el log y `syncActivityLogs` lo envía con reintentos (backoff exponencial 15s·2ⁿ, tope 5min; se dispara al reconectar/montar/cada 15s desde `page.tsx`). Al confirmar la DB se borra; **400/401/403 = permanente** (queda `'error'` con razón, no reintenta); **red/5xx = temporal** (reintenta). Puntos instrumentados: imprimir Dashboard, imprimir Registrados, y exportar Excel de Registrados/Familias/Individuos y de Morbilidad. **Excel de Registrados = modal con 3 variantes:** General (con filtros), Familias (`exportFamiliasExcel`: agrupa por núcleo, jefe resaltado + color por familia, sin filtros) e Individuos solos (sin filtros). Requiere ampliar el CHECK de `AuditLog` → `prisma/auditlog_acciones.sql` (manual en Supabase); si no se corre, el log falla en silencio y la impresión/descarga sigue funcionando.
- **Preview / service worker:** `public/sw.js` cachea agresivo; se registra **solo en producción** (`layout.tsx`). Su `BUILD_TS` se **autogenera** con el commit SHA en cada build (`scripts/update-sw-version.mjs`, vía el script `prebuild` de `package.json`), así cada deploy invalida el cache de los clientes sin editarlo a mano. El navegador headless del preview puede servir chunks viejos → si el cambio no se refleja, borrar `.next` y reiniciar.
- **Vercel serverless:** no confiar en estado en memoria entre requests; el cache de sesión de auth es por-instancia (ver [[project-deployment-vercel]] en la memoria).

## Tailwind (migración progresiva)

**Tailwind v4** está instalado **sin Preflight** (decisión del equipo). ⚠️ v4 requiere navegadores ~2023+ (Safari 16.4, Chrome 111, Firefox 128) porque usa `@property`/`color-mix()`/cascade layers; si aparecen dispositivos muy viejos que no rendericen bien, evaluar volver a v3. Con Preflight omitido, Tailwind **no resetea nada** — convive con `globals.css` sin alterar el diseño; las utilidades solo se generan al usarlas.

- **El sistema base sigue siendo el CSS con variables** (`globals.css`). Tailwind es para código **nuevo** o para migrar componentes **de a poco**. NO hacer una migración masiva de golpe.
- **Colores mapeados** (bloque `@theme inline` en `globals.css`): `bg-primary`, `text-danger`, `border-success`, `bg-gold`, etc. resuelven a las variables del sistema y respetan claro/oscuro. Úsalos en vez de valores hardcodeados.
- **Config (estilo v4, en CSS — no hay `tailwind.config.js`):** al inicio de `src/app/globals.css` se importan **solo** las capas `theme` y `utilities` (se **omite `preflight.css`** para no resetear) y sigue el bloque `@theme inline`. El plugin es `@tailwindcss/postcss` en `postcss.config.mjs`.
- **Al migrar un componente:** reemplaza sus clases custom por utilidades Tailwind y borra del `globals.css` el CSS que quedó sin uso (evita duplicación). `npx tsc --noEmit` no valida CSS — comprueba el resultado con `next build` y visualmente.

## Gotchas / lecciones aprendidas

- `FormData` choca con el tipo global del DOM → importar de `@/types`.
- `saveLocal` debe preservar TODOS los campos (`type`, `refugio`, `userId`) o se pierden.
- Badges/gating nuevos: **incluir MASTER** (varios ternarios asumían solo 3 roles).
- El padrón NO debe restringirse a Master/Admin en `download` (el Registrador lo necesita para lookup).
- **Salones/capacidad:** los `CustomRoom` se distribuyen al cliente como `customCuartos` (string[] de nombres, para no romper `allCuartos`) **+** `roomCapacities` (mapa nombre→camas, default 18), ambos en `page.tsx` y por el context. El cache local se **sella por refugio** (`cuartos_owner`) y el fetch pide `?refugio` del usuario para que Master no vea los salones de todos los refugios. La capacidad se lee en el select de asignación y en la distribución por habitación (color proporcional vía `roomFillLevel` en `helpers.ts`, no un 18 fijo). El nombre canónico es `"<CONTENEDOR> SALON <n>"`: el contenedor es `EDIFICIO n`/`PISO n` (se elige el tipo Edificio/Piso/Otro al crear en Config) o texto libre; Config agrupa los salones por contenedor. El censo (paso 4) tiene un select de habitación **opcional** con el mismo semáforo. **Refugio de vista (Master):** Master cambia el refugio activo desde el **selector del header** (`viewRefugio` en `page.tsx` → `effectiveRefugio`, con `effectiveRefugioRef` para evitar stale closures); afecta TODO — dashboard, registrados, salones y censo. El resto de usuarios ve siempre su refugio. Las lecturas mandan `?refugio=effectiveRefugio`; el backend usa `refugioScopeFor(auth, requested)` (`/api/registros` y `/api/stats` lo aceptan para Master, retrocompatible: sin el parámetro Master ve todos). Config y el censo consumen `effectiveRefugio` del context; los usuarios (`/api/auth/users`) NO se filtran por la vista (Master los gestiona globalmente).
- **Refugio obligatorio:** un usuario sin refugio (`campamentoTransitorio` vacío/nulo) NO puede censar (`/api/register` → 403) NI puede crearse/editarse como usuario (`/api/auth/users` POST/PUT → 400). Guarda `hasRefugio` en `auth.ts` (backend, fuente de verdad) + espejo en `permissions.ts` (UX: banner en el censo y validación en el form de usuarios). Master registra en su propio refugio si no especifica otro (ya no cae a un default hardcodeado).
